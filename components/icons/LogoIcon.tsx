
import React from 'react';

const LogoIcon: React.FC<React.HTMLProps<HTMLImageElement>> = (props) => (
    <img 
        src="https://otash2002.github.io/prezto_pizza_bot/img/logo.png" 
        alt="Presto Pizza Logo" 
        {...props} 
    />
);

export default LogoIcon;
